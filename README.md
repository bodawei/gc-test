# gc-test

## Consonant Counter

A single static page that counts the number of consonants in text as you type.

Open `index.html` directly in a browser — no build step or server required.

GitHub Pages URL — add once enabled.